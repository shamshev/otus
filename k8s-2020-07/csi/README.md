# demo01

`kubectl apply -f 01_pod_w_volume`
`kubectl get pods`
`kubectl exec -it demo01 /bin/sh`
`df -h`

node:
`df -h`
`docker ps | grep test`
`docker inspect id | less`

see HostConfig - Binds

`cd dir`
`touch file`

see file in container:

`free -m` host & container